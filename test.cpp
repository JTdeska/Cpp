#include <iostream>

using namespace std;
int n,k;
vector<int> v;

long getMaxSum(int i, int k)
{
    if()
}

int main(){
    int t;

    cin>>n>>k;
    for(int i=0;i<n;i++)
    {
        cin>>t;
        v.push_back(t);
    }

    cout<<getMaxSum(0, k)<<endl;
    return 0;
}